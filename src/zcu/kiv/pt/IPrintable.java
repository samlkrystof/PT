package zcu.kiv.pt;

/**
 * interface for instances which need printInfo method
 * to print out some info about them
 */
public interface IPrintable {

    /**
     * method prints out info about this instance
     */
    void printInfo();
}
